// Constantes de durées utilisées dans l'application
class AppDurations {
  AppDurations._();

  static const Duration pageTransition = Duration(milliseconds: 300);
  static const Duration snackbar = Duration(seconds: 2);
  static const Duration countdownTotal = Duration(seconds: 5);
  static const Duration countdownUpdate = Duration(milliseconds: 50);
  static const Duration buttonClickDelay = Duration(milliseconds: 75);
  static const Duration buttonClickDelayLong = Duration(milliseconds: 250);
  static const Duration permissionRequestDelay = Duration(milliseconds: 1000);
  static const Duration permissionDialogDelay = Duration(milliseconds: 500);
  static const Duration navigationResetDelay = Duration(milliseconds: 5);
  static const Duration rideauFrame = Duration(milliseconds: 60);
  static const Duration rideauAnimationComplete = Duration(milliseconds: 100);
}
